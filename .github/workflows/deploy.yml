name: Deploy (Local)
on: 
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Deploy (Local)
        shell: bash
        uses: appleboy/ssh-action@master

        with:
          host: ${{ secrets.HOST2 }}
          
          script: |
            set -e
            cd /home/lucci/Escritorio/mi_examen
            git pull