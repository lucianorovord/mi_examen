name: Deploy (Local)
on: 
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Deploy (Local)
        shell: ${{ secrets.SHELL }}

        run: |
          set -e
          cd /home/lucci/Escritorio/mi_examen
          git pull