name: Deploy (Local)
on: 
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Deploy (Local)
        with:
          host: ${{ secrets.USER }}
          
          run: |
            cd /home/lucci/Escritorio/mi_examen
            git pull

